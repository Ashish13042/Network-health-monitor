# Network-health-monitor
A Python script that periodically pings a list of IP addresses (routers, servers, or hardware tools), checks their status, and logs the response times. If a device goes down or response times spike, the script automatically triggers an alert by sending an email.
